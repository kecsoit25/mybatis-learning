<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 1. properties -->
	<properties resource="config.properties">
		<property name="username" value="m2dbtest" />
		<property name="password" value="password123" />
	</properties>
	<!-- 2. typeAlias -->
	<typeAliases>
  		<typeAlias alias="MyObject" type="test.test1.mybatis.pojo.MyObject"/>
  		<typeAlias alias="MyObject2" type="test.test1.mybatis.pojo.MyObject"/>
  	</typeAliases>
  	<!-- 另外一种alias的方式 -->
  	<!-- 注意注解 @Alias 的使用
  	<typeAliases>
  		<package name="domain.blog"/>
	</typeAliases> 
	-->
   <!-- 3 environments -->
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<!-- 配置数据库连接信息 -->
			<dataSource type="POOLED">
				<property name="driver" value="${driver}" />
				<property name="url" value="${dburl}" />
				<property name="username" value="${username}" />
				<property name="password" value="${password}" />
			</dataSource>
		</environment>
		<environment id="myown">
			<transactionManager type="JDBC" />
			<!-- 配置数据库连接信息 -->
			<dataSource type="POOLED">
				<property name="driver" value="${driver}" />
				<property name="url" value="${dburlmyown}" />
				<property name="username" value="${username}" />
				<property name="password" value="${password}" />
			</dataSource>
		</environment>
	</environments>

	<mappers>
		<!-- 注册userMapper.xml文件， userMapper.xml位于me.gacl.mapping这个包下，所以resource写成me/gacl/mapping/userMapper.xml -->
		<mapper resource="test/test1/mybatis/mapping/userMapper.xml" />
		<mapper resource="test/test1/mybatis/mapping/myobjectMapper.xml" />
	</mappers>

</configuration>